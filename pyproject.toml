[project]
name = "trim-mp3-cover-art"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "colorlog>=6.10.1",
    "mutagen>=1.47.0",
    "pillow>=12.1.1",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.19.0",
]
